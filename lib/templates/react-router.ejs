import {lazy} from 'react'

// 普通加载路由
import <%= data.name %> from './<%= data.name %>.tsx'
// 懒加载路由
const <%= data.name %> = lazy(() => import('./<%= data.name %>.tsx'))
export default {
  path: '/<%= data.lowerName %>',
  name: '<%= data.name %>',
  element: <<%= data.name %>/>,
  children: [
  ]
}